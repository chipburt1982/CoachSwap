{
  "name": "coachswap",
  "version": "1.0.0",
  "description": "A peer-to-peer marketplace for football coaches to trade, sell, and buy used equipment",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"cd frontend && npm run dev\" \"cd backend && npm run dev\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "test": "npm test --prefix backend && npm test --prefix frontend",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx"
  },
  "keywords": [
    "marketplace",
    "football",
    "coaches",
    "equipment",
    "trading",
    "peer-to-peer"
  ],
  "author": "chipburt1982",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "eslint": "^8.54.0"
  }
}
